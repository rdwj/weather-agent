apiVersion: v1
kind: Service
metadata:
  name: weather-agent-ui
  namespace: weather-agent
  labels:
    app: weather-agent
    component: ui
spec:
  selector:
    app: weather-agent
  ports:
    - name: streamlit
      port: 8501
      targetPort: 8501
      protocol: TCP
  type: ClusterIP