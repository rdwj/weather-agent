apiVersion: v1
kind: Secret
metadata:
  name: weather-agent-secrets
  labels:
    app: weather-agent
    app.kubernetes.io/name: weather-agent
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # MCP Server Configuration
  MCP_URL: "https://mcp-server-weather-mcp.apps.cluster-sdzgj.sdzgj.sandbox319.opentlc.com/mcp/"
  MCP_TIMEOUT: "30"

  # LLM Configuration (Optional - for full features)
  # Replace these with your actual values
  LLM_URL: "YOUR_LLM_ENDPOINT_HERE"
  LLM_API_KEY: "YOUR_LLM_API_KEY_HERE"
  LLM_MODEL_NAME: "YOUR_MODEL_NAME_HERE"

  # Redis Password (if configured)
  REDIS_PASSWORD: ""

  # Additional security tokens if needed
  API_SECRET_KEY: "GENERATE_A_SECURE_KEY_HERE"